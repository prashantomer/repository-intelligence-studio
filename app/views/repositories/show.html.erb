<% content_for :title, @repository.name %>

<%= render "repositories/show_content",
           repository: @repository,
           latest_ingestion: @latest_ingestion,
           recent_ingestions: @recent_ingestions,
           recent_audit_logs: @recent_audit_logs,
           code_files_count: @code_files_count,
           code_chunks_count: @code_chunks_count,
           entities_count: @entities_count,
           routes_count: @routes_count,
           relationships_count: @relationships_count,
           dependency_edges_count: @dependency_edges_count,
           dependency_edge_breakdown: @dependency_edge_breakdown %>
